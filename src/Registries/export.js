const { BlockRegistry } = require('./BlockRegistry')
const { FluidRegistry } = require('./FluidRegistry')
const { RecipeRegistry } = require('./RecipeRegistry')
const { ItemRegistry } = require('./ItemRegistry')
const { EntityRegistry } = require('./EntityRegistry')
module.exports = {
    EntityRegistry,
    FluidRegistry,
    ItemRegistry,
    RecipeRegistry,
    BlockRegistry
}
